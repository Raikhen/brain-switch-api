type Link {
  id: ID! @unique
  title: String!
  url: String!
  createdAt: DateTime!
  postedBy: User!
}

type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
  links: [Link!]!
}
